# AI_Document_Search
A web app where users upload PDFs and then chat with them: the system finds the most relevant passages using semantic search and feeds those passages to an LLM to produce grounded, cited answers.
